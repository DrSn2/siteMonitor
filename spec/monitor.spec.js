//var monitor = require('../monitor');

describe("The Monitor", function() {
    it("should contains spec with an expectation", function() {
//        expect(true).toBe(true);
//        console.log('monitor: ' + monitor.buyViaCounter);
//        monitor.buyViaCounter == 0;
    });
});